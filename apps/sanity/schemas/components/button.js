import CreateComponent from "../../utils/CreateComponent";

export default CreateComponent({
  name: "button",
  title: "Button",
  fields: [
    {
      type: "string",
      name: "content",
      title: "Content",
    },
  ],
});
