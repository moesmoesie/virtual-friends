{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },

    "sanity-deploy": {
      "cache": false,
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },

    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
